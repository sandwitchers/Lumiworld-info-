{
  "name": "lumiworld-info",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Lumiverse Spindle port of SillyTavern WorldInfoInfo (AGPL-3.0).",
  "scripts": {
    "build": "bun build src/backend.ts --outfile dist/backend.js --target bun && bun build src/frontend.ts --outfile dist/frontend.js --target browser --format esm",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "lumiverse-spindle-types": "^0.6.29",
    "typescript": "^7.0.2"
  }
}
